# Build artifacts
build/
build-log-*.txt

# IDF Component Manager - fetched per-clone, locked by manifest
managed_components/
dependencies.lock

# Generated sdkconfig (regenerated from sdkconfig.defaults on first build)
sdkconfig
sdkconfig.old

# components/ contents are vendored snapshots (was junctions to sibling
# arduino-cli repo + arduino libraries dir). To be restructured next
# session: TI_* layers consolidate into a single TI_EB_Emulator_ESP repo,
# third-party Arduino libs become git submodules of forks.

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.DS_Store
